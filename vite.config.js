import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    open: true,  // Apre automaticamente il browser
  },
  root: './',  // La cartella principale del progetto (è già corretto così)
});
